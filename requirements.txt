# Computer vision + landmarks
mediapipe==0.10.14
opencv-python-headless>=4.9.0
numpy>=1.24.0,<2.0
Pillow>=10.0.0

# Face analysis (emotion, age, gender, recognition via Facenet512)
deepface>=0.0.89
tensorflow-cpu>=2.16.0,<2.19.0

# Clustering
scikit-learn>=1.4.0

# Local vector database (cross-session face store + similarity search)
chromadb>=0.5.0,<1.0.0

# Local LLM inference (optional — falls back to rule-based if Ollama not running)
ollama>=0.3.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.3

# Misc
tqdm>=4.66.0
